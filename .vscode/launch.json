{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Arranque Local de Medilink en VS Code (Angular)",
      "type": "chrome",
      "request": "launch",
      "preLaunchTask": "npm: start",
      "url": "http://localhost:4200",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
